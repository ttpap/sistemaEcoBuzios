# Copie este arquivo para .env.local (dev) ou .env.production (deploy)
# e preencha com os valores do seu projeto Supabase.
#
# Supabase → Project Settings → API
# - Project URL
# - Project API keys → anon (public)

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
