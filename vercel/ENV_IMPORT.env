# Importar na Vercel (Settings → Environment Variables → Import .env)
#
# ETAPA 1 (ADR-001): o app deve apontar para UM Supabase oficial via env.
# Substitua pelos valores do seu projeto em Supabase → Project Settings → API.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=